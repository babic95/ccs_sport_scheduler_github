// Notifications.js
import React from 'react';
import { Box, Button, Typography } from '@mui/material';

const NewNotification = ({ notifications, handleNotificationClick }) => {
    return (
        <Box>
            
        </Box>
    );
};

export default NewNotification;